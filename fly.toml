app = "work-classifier-bot"
primary_region = "waw"  # Warsaw - close to Ukraine

[build]

[env]
  # Add TELEGRAM_BOT_TOKEN via fly secrets set

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
